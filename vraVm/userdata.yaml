runcmd:
  - "hostnamectl set-hostname testHostName"
groups:
  - group: sudo
users:
  - name: matthias
    plain_text_passwd: VMware1!
    lock_passwd: false
    ssh_pwauth: 'yes'
    sudo:
      - 'ALL=(ALL) NOPASSWD:ALL'
    groups: sudo
    shell: /bin/bash
